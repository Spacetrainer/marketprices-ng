- [Stage 1] CLAUDE.md summary lists "grid 972px" but architecture §13 defines no --grid token (972 appears nowhere). tokens.css written faithful to §13. Reconcile before the first component that uses the main content grid: either add --grid:972px to §13 then tokens.css (per P6.2, doc first), or drop "grid 972px" from the CLAUDE.md summary. Decision pending.
- [Primitives] Focus rings on Button/FilterPill rely on the browser's native :focus-visible outline — no outline: none is set anywhere, which satisfies P7.5 literally, but no dedicated focus-ring token exists in tokens.css and none was invented. Revisit at Stage 21 to decide whether focus rings get an explicit token (e.g. an amber ring) instead of the native default.
- [Primitives] Button's spec padding (§4.7) is "10px 24px". --sp-6 (24px) matches the horizontal value exactly; no --sp-* token matches 10px (scale jumps --sp-2:8px → --sp-3:12px). Implemented as an arbitrary py-[10px] literal (not a color, so P6.1's hex-ban doesn't apply) rather than rounding to an existing token and drifting from spec. Reconcile if/when a token is added for this value.
- [Layout chrome] tokens.css has no --breakpoint-* tokens at all. §11.2's four breakpoints (1440/1200/992/768) and §4.8's two nav-spacing values (36px/28px) exist only as prose, not tokens. Nav and the chrome components use them as literal arbitrary Tailwind values (min-[992px]:, max-[767px]:, gap-[28px], gap-[36px]) — spec-sourced, not invented, but not reconciled into tokens.css/@theme as reusable breakpoint tokens yet. Reconcile before a second component needs the same breakpoints (per P6.2, doc first).
- [Layout chrome] `pnpm test:e2e` (Playwright) cannot run in this Codespace sandbox — the downloaded Chromium binary is missing ~20 system libraries, and apt can't locate most of them without a package-index refresh, which needs elevated/unsandboxed permission this session doesn't have. The suite (tests/e2e/chrome.spec.ts) is written and kept; it runs in CI instead (.github/workflows/ci.yml), where `playwright install --with-deps` works normally on the GitHub-hosted runner. Verified locally via pnpm dev + compiled-CSS/DOM inspection instead — see the §1.4 commit message/PR notes for what was checked.
- [Entitlements] api_key_hash and api_rate_limit_per_day, both originally planned for the entitlements table, are dropped entirely from 0004_entitlements.sql. API-key issuance/storage and rate-limit enforcement are deferred to a future api_keys table — no client-facing API surface exists yet to consume either field, and storing a hash column with nothing populating or checking it is worse than not having it. Revisit when the API-key feature is actually built: add api_keys (key_hash, entitlement_id/user_id, rate_limit_per_day, ...) as its own migration rather than reviving these columns on entitlements.
- [Tags/pins] P9.1 enumerates the tables that may carry an anon SELECT policy ("only on": sections, commodities, units, collection_sites, price_observations, basket_snapshots where is_complete, articles where published). 0014_tags.sql amends that list by THREE, consciously: `tags` and `article_tags` because /tag/[slug] is a public page rendered as a join-table query (§13 placement map) and neither table is readable by a signed-out visitor without it; `homepage_pins` because getSectionArticles() applies pins before falling back to recency and runs on the public homepage under ISR. The homepage_pins policy is scoped to `expires_at > now()` so an expired pin is unreachable rather than merely unrendered; the other two are unscoped, since a tag is a slug and a display name and a join row exposes only that some article id carries some tag (the article itself stays behind articles' published filter). The alternative considered for homepage_pins — routing the homepage query through a service-role client — was rejected: it makes every homepage render depend on a privileged connection for one small table, to avoid exposing a uuid that cannot be resolved to a person because profiles is not anon-readable. Revisit if P9.1's list is ever restated: it should be amended to name these three, or these policies should be narrowed.
- [Articles] articles.type shipped in 0013 with two values ('standard', 'video'); §1.2 and §1.3 both specify three, the third being 'data' (card treatment "chart or table preview instead of a photo", body "rich text + embedded price widget"). 0013 was already applied when this was found, so the fix is 0015_article_type_data.sql — a widening of the existing `articles_type_check` constraint, reusing the same constraint name — rather than an edit to 0013. No exemption was created for 'data' articles: header_media_id stays NOT NULL, because §1.3's "instead of a photo" describes the card, while §1.4's "every content item carries a designed graphical header, rendered from a template against bound data" is unqualified and a rendered chart is precisely that.
- [Homepage] NOTED GAP, blocks Stage 12 — no table anywhere holds the hero. §1.5 says `hero_lead`, `hero_secondary_1` and `hero_secondary_2` are manually curated with a fallback to most-recent-across-all-sections after 24h. That string appears exactly once across all three docs and §9 defines no table for it. homepage_pins cannot serve: it is keyed on section_id and the hero is explicitly across all sections, and its expiry window (72h, configurable) is a different rule from the hero's 24h fallback. Stage 12's HeroMosaic therefore has no data source. Needs its own decision and migration before Stage 12; nothing built in 0014.
- [Videos] `videos.published_at` is renamed to `youtube_published_at` in 0016_videos.sql, diverging from §9.1's printed column list. `published_at` is not a wrong name for the field — it is a correct name for a different fact, and that is the problem. Across this schema `published_at` already means the moment WE released something to OUR readers (articles.published_at, written by the publish cron and governed by P5.1). On `videos` it would mean the moment a video appeared on the YouTube channel. Those never coincide: a video synced today may have gone up eighteen months ago, and the article carrying it publishes later still on its own schedule. The collision is silent rather than noisy — both columns are timestamptz, both sort descending in a "newest first" list, and a query joining videos to articles can say `published_at` and be answered by either table with no error, only a wrong answer. "Sort the WATCH band by published_at" is a sentence a person could write meaning either one. A comment warning about the ambiguity only helps the person who reads it; a name that cannot be confused helps the one who does not. Also renamed in the same migration, on weaker grounds: §9.1's `duration` → `duration_seconds`, integer, nullable, `> 0` when present — the spec gives a bare untyped name, which is exactly how an untyped `duration` becomes an interval in one reader's head, a "4:31" string in another's and YouTube's ISO-8601 "PT4M31S" in the API response; naming the unit ends that and the conversion happens once at the sync boundary. Revisit only if §9.1 is ever restated, in which case it should adopt both names rather than these columns reverting.
- [Anomalies/signals] TWO PLACES STILL REFERENCE `content_items` WITHOUT CREATING A ROW IN IT, reduced from four when 0018_content_items.sql landed. WHAT CLOSED: `articles.content_item_id` (0013) now carries a real foreign key AND is NOT NULL, applied in 0018 while the table was still empty, which makes P1.9 structural rather than aspirational — note that 0013's `protect_article_provenance()` deliberately permits null → value on that column so the gap could be backfilled, and that branch is now unreachable; it is harmless and stays, recorded here so it is not later read as a puzzle. `videos.imported_content_item_id` (0016) now carries its foreign key too, so 0016's partial unique index finally sits on a uuid that is checked to resolve. WHAT REMAINS OPEN, and why it is a decision rather than an oversight: (1) `promote_price_anomaly()` (0011) and (2) `promote_signal()` (0017) both still flip state and nothing else, so Promote on Price radar and on Signal feed is two steps in application code rather than one transaction. Creating the row needs format, insight_type, archetype, header_template, a non-empty sources array, headline_hypothesis and the four format-fit integers; insight_type is derivable from what matched and format arrives from the split button, but punch, explanation_load, progression, stakes and video_fit have no established source — see the format-fit gap entry below. Both are fixed forward in one later migration once that question is settled, collapsing each promote into a single transaction.
- [Engine] The build plan's migration table names this file `0015_engine_sources.sql`, assuming the engine batch landed before the article batch. It did not: 0015 is `0015_article_type_data.sql` and 0016 is `0016_videos.sql`, so the engine spine ships as `0017_engine_sources.sql` (Stage 2, migration batch 6). THE OFFSET IS NOW TWO AND IS SYSTEMATIC, not a one-row slip: the plan's `0016_content_items.sql` ships as `0018_content_items.sql` (batch 7), and every remaining plan row — distribution, measurement, governance, functions, triggers, RLS, indexes — is displaced by the same two. AND THE OFFSET IS NO LONGER A CONSTANT: the plan's single `0017_distribution.sql` splits into two files under P10.8, of which only `0019_content_performance.sql` (batch 8) has shipped — the social_content_queue half is unnumbered and out of the chain, see the entry below. From here the plan's table cannot be reconciled by adding two to every row; it has to be rewritten against what the repo contains. No schema consequence — the file is numbered by what the repo actually contains, which is the rule that matters — but the plan's migration table is now wrong for this row and will be wrong for every later engine row too (`content_items`, `site_events`). Reconcile the plan's whole migration table in one pass rather than rediscovering the offset once per migration.
- [Engine] `sources.active` is renamed to `is_active` in 0017_engine_sources.sql, and `content_templates.active` likewise in 0018_content_items.sql, both diverging from §9.3's printed column lists. Every other table in this build spells the flag `is_active` — collection_sites, profiles, commodities, units, collectors — and a single table spelling it differently propagates the exception into every join, every query in lib/queries/ and every line of types/database.ts, permanently. Same disposition as 0011's `window` → `comparison_window` and 0016's `duration` → `duration_seconds`: §9.3 should adopt the name if it is ever restated, rather than either column reverting.
- [Engine] `decision_utility` SCALE DISCREPANCY, logged rather than resolved. §8.3 states decision_utility "is scored 0–1 by the classifier against a fixed five-point rubric". §8.10 specifies the Dashboard's "Does decision utility predict engagement?" chart as items binned by `decision_utility` at promotion into 0–2, 2–4, 4–6, 6–8, 8–10 — a 0–10 scale. The column in 0017 is `numeric check (decision_utility between 0 and 1)`, following §8.3, because that is where the scoring rubric is defined and because it matches the whole 0–1 score family beside it (geo_score, recency_score, novelty_score, commodity_match). §8.10's bins are therefore unbuildable as printed and must be restated as 0–0.2, 0.2–0.4 …, or rendered by multiplying by 10 at the chart boundary. NOT resolved here: picking one silently would leave the other document wrong with nothing recording that it is. Decide before §8.10's Dashboard chart is built; whichever way it goes, the constraint in 0017 is the thing to check against.
- [Content items] `header_alt` CANNOT BE NOT NULL, contrary to P7.2 [M] ("`content_items.header_alt` is `NOT NULL`; a rendered header graphic is an image and the engine does not get to be the loophole"). P7.2 and §8.9 were written from different assumptions about when the row comes into existence. §8.9 creates it at promote, before the chain runs, and the header does not exist until article Pass 7 or video Step 5 — so at `queued` there is no image for alt text to describe. Satisfying a literal NOT NULL would mean auto-writing placeholder text into a saved field, which P3.3 forbids outright, describing an image that does not exist. 0018 therefore makes the column nullable and requires it in the allocation check, alongside slug, section_id and headline, before any of `scheduled`/`published`/`dispatching`. P7.2's intent is preserved in full — no header reaches a reader without alt text — and what is given up is only the always-on column-level constraint. Revisit if P7.2 is ever restated: it should state the requirement at publish rather than at insert.
- [Content items] `video_fit` SCALE vs THRESHOLD INCONSISTENCY, logged rather than resolved, same treatment as the `decision_utility` entry above. §8.5 defines `video_fit = (progression × 0.35) + (punch × 0.30) + (stakes × 0.25) + (recency × 0.10)`, a weighted sum of four 0–10 inputs whose weights total 1.0, so the maximum possible value is 10. The recommendation rule on the next line then tests `video_fit ≥ 75`, which no value of that formula can ever reach — as printed, the rule recommends video never. 0018 bounds the column `between 0 and 100`, following the threshold, because 75 is the number the rule actually compares against and a bound of 10 would refuse every value that rule accepts. NOT resolved here: either the inputs are rescaled to 0–100 before the sum, or the threshold is 7.5, and picking one silently would leave the other reading wrong with nothing recording it. Decide before the format-fit scorer is built; whichever way it goes, the constraint in 0018 is the thing to check against.
- [Content items] FORMAT-FIT SCORES HAVE NO HOME BEFORE PROMOTION — NOTED GAP, blocks the two remaining promote functions above. §8.11 specifies the Signal feed's promote split button as rendering the `video_fit` figure plus a line reading `Punch 9 · Explanation 6 · Progression 8 · Stakes 9`, so an editor picks a format while reading five numbers. §9.3 places all five on `content_items` and nowhere else, and `signals` (0017) has no such columns; there is no `insights` table anywhere in §9, and §9.7 records `agent_drafts` as superseded by `content_items` rather than replaced by an insight store. Since the row is born at promote (§8.9), those figures are displayed before the row that holds them exists. Two readings work and they are not equivalent: (a) fusion computes them on demand each time the feed renders and promote passes the displayed values into the new row, or (b) a store is missing from §9.3. 0018 proceeds on (a) as the reading that needs no new table, and records the cost: under (a) two renders can legitimately differ if a coefficient changed between them, and the figure the editor acted on is not reconstructable afterwards — a weaker position than §8.5's own stated goal of storing format fit "so the recommendation is auditable rather than a black box". Same shape as the homepage hero gap above: a spec'd screen behaviour with no spec'd data source. Settle before rewriting the promote functions.
- [Content items] `content_items.slug` carries NO uniqueness while `articles.slug` is globally unique (0013). Deliberate, not an omission: two queued drafts may legitimately hold the same working slug while both are still being written, and refusing the second would be refusing work that has not yet made any public claim. The collision surfaces at publish, where `articles.slug`'s unique index is the real gate. Consequence to be aware of when the publish job is built: a slug clash is a publish-time failure that must set `status = 'failed'` with the clash named, exactly like a missing allocation field, rather than an unhandled constraint violation in the cron.
- [Distribution] BLOCKING — `social_content_queue` DOES NOT EXIST in the project this repo migrates, so the ALTER that P10.8 and §9.4 describe cannot be written at all. Verified three ways on 2026-08-27: `types/database.ts`, regenerated after 0018, lists 25 tables and it is not among them; a catalogue listing of the public schema in project `marketprices-rebuild` returns the same 25, every one of them created by migrations 0002–0018 and all with zero rows; and a re-check across nine further candidate schemas (private, app, agent, agents, content, social, engine, internal, staging) finds nothing. The only other project in the account is an unrelated product and has no such table either. CAUSE: the rebuild started on a Supabase project created 2026-08-13, and the live Agent 2 / Agent 3 pipeline runs against the pre-rebuild database, which is not in this account. §9.4's "EXISTING LIVE TABLE. Add two columns; do not recreate (M13)", CLAUDE.md's "LIVE table used by Agent 3 in production" and P10.8's whole framing were all written before that move and none was updated. THREE POSSIBLE REAL STATES, needing different work: (a) the table lives in a different database entirely, in which case this migration can never belong to this repo's chain, §8.15 step 4's "insert rows into the existing social_content_queue" needs rewriting as a cross-system write, and §9.4's `social_content_queue.content_item_id` can never carry a foreign key to `content_items`; (b) the table is migrated into this project at cutover, in which case the correct statement is CREATE TABLE with both columns present from birth and P10.8's caution attaches to the cutover rather than to a column add; (c) it is expected here and simply has not been created yet. Nothing is drafted, numbered or staged for that table until this is answered — deliberately, because a confidently wrong migration against a live queue is the exact outcome P10.8 exists to prevent.
- [Measurement] THREE DEPARTURES FROM §9.4 on `content_performance` in 0019, consolidated here as one entry. (1) `avg_time` is renamed `avg_time_seconds`, the same call and the same reasoning as 0016's `duration` → `duration_seconds`: §9.4 gives a bare untyped name, which is how an untyped average time becomes seconds in one reader's head, minutes in another's and a Postgres interval in a third's. §8.10's ten-minute cap on average read time is a collector rule and a versioned setting, deliberately NOT a constraint on the column, so the raw figure stays representable. (2) `read_completion` and `watch_through` are stored as FRACTIONS bounded 0–1, not percentages, with the unit stated in the column comment. §8.10 writes "read completion at 80% depth", which reads as a percentage, so this is a real ambiguity resolved rather than an obvious reading: 0–1 matches the existing score family across `signals` and the formatting layer multiplies for display, which is the direction of conversion used everywhere else in this schema. (3) `collected_at timestamptz not null default now()` is ADDED, and is not in §9.4's printed column list at all. It is the companion to `collected_for_date` — one says which day the figure describes, the other when we last went and asked — added on the same reasoning that gives `sources.last_polled_at` and `last_error` their place in 0017: without it, a source that is genuinely lagging and a collector that has silently stopped running are indistinguishable, because both present as a newest row describing three days ago. P18.4's freshness statement is answerable from `collected_for_date` alone; telling lag apart from failure is not. It is deliberately excluded from the uniqueness key, since a value that changes on every refresh would make each re-fetch a new row and defeat the key. Revisit all three together if §9.4 is ever restated — it should adopt `avg_time_seconds`, state the fraction convention and print `collected_at`, rather than these columns reverting.
- [Measurement] FOUR DEPARTURES FROM §9.4/§9.5 in 0020_measurement.sql, consolidated as one entry. (1) `site_events` HAS NO SELECT POLICY — not for staff, not for anyone — which is stricter than P9.1's "everything else is authenticated-only" and is the one place in the build where a table is deliberately unreadable through the application client. P8.7 says "the Dashboard never scans `site_events`", and a staff SELECT policy would make `from('site_events')` work perfectly from a Dashboard query: fast at launch, progressively slower every week the product succeeds, on the one screen whose Zone 1 must render in under a second. Withholding the policy makes the violation impossible rather than discouraged; the sanctioned path, `daily_rollups`, carries the usual `is_staff()` policy. COST, accepted knowingly: an operator debugging the beacon needs a service-role connection to see the table at all. Reversible in one line if the rollup job ever needs a non-service-role reader. Revisit if P9.1 is restated — it should name this table as the exception, rather than the policy being added. (2) `site_events.session_id` is typed `uuid`, where §9.4 gives no type. The reasoning is privacy rather than tidiness: a uuid column cannot hold an email, a phone number, an IP or a signed-in user's id, so whatever a future implementer is tempted to correlate sessions by, the column refuses it at the boundary instead of silently storing it. `text` would accept all of them. (3) `daily_rollups.computed_at` is ADDED and is not in §9.4's printed column list, on identical reasoning to `content_performance.collected_at` in 0019 — `date` says which day the row aggregates, `computed_at` says when the job last wrote it, and without the second a rollup job that died at 09:00 and one that has seen no traffic since 09:00 produce identical tables (P18.4). Excluded from the primary key for the same reason 0019 excludes `collected_at` from its uniqueness key. Also, §9.4's printed `UNIQUE (date, metric_key, dimension, dimension_value)` ships as the four-column PRIMARY KEY with no surrogate `id`: with all four columns NOT NULL the two are equivalent in what they enforce, no table references a rollup row, and the composite is what every read and every upsert uses. (4) `weight_proposals.status` has a four-value vocabulary — `proposed`, `accepted`, `rejected`, `superseded` — where §9.5 prints the column with no values. `superseded` is the load-bearing one: the partial unique index `weight_proposals_one_open_per_setting` allows only one open proposal per (scope, key), so a weekly proposer needs somewhere to put the previous suggestion that is neither a decision a human made nor a deletion (P1.4). `accepted` means "a human agreed", NEVER "this is in effect" — the applied change is a versioned `editorial_rules` row written by a person through Settings with an audit diff (P16.2, P16.5, P16.6), and reading this column as the live state of a setting is the single misreading that would turn the table into the self-tuning system P16.5 forbids. Two related non-departures worth recording so they are not later read as omissions: `weight_proposals` carries NO foreign key to `editorial_rules` and cannot ever, because that table is versioned and many rows share a (group, key) — the pointer is textual and the Settings screen must handle resolving to nothing; and `sample_size` is NOT NULL with `> 0` but carries neither §8.10's 50-item sample nor P18.2's data floor as a constraint, on the same reasoning 0019 gives for §8.10's ten-minute read-time cap — those are versioned settings the proposer reads, and a threshold in a check constraint is a threshold only a migration can change, which is P16.1 inverted.
- [Measurement] NOTED OPEN ITEM, no schema consequence yet — `site_events` SHIPS WITH NO RETENTION POLICY AND NO PARTITIONING, and it is the only table in the build that grows with traffic rather than with editorial work. Every other table grows with a week, an article, a signal or a submission; this one grows with the number the whole product is trying to increase. Nothing in build-protocol.md, the build plan or the architecture document states how long raw events are kept: §9.4 defines the table, §9.6 gives it one index annotated "rollup job only", and P8.7 says the Dashboard never reads it — that is the entire treatment. Deciding it inside 0020 would have been inventing a policy, and the four plausible answers are different table shapes rather than different cron jobs: drop after N days (a cron plus an index on created_at alone), partition by month (a rewrite, and the partition key's granularity is a second decision), roll up then truncate (needs the rollup vocabulary to be settled first, which it is not until Zone 3 is built), or keep everything (a cost decision nobody has been asked to make). WHAT BUYS THE TIME: `daily_rollups` makes the raw log discardable in principle from day one — every Dashboard figure already reads the aggregate, so a retention rule can be introduced later without touching a single query. WHAT MAKES IT URGENT LATER RATHER THAN NEVER: partitioning an existing events table is a rewrite whether it happens at ten million rows or a hundred million, so the choice gets more expensive monotonically and never gets easier. Settle before the table has a year of traffic in it, and settle retention BEFORE partitioning — choosing a partition key without knowing what is being kept is choosing the second half of a decision before the first. Whichever way it goes, it lands as its own migration; nothing in 0020 assumes an answer.
- [Measurement] SECURITY FINDING, FIXED FORWARD BY 0023 — `protect_weight_proposal()` (0020_measurement.sql) NEVER PROTECTED `decided_by` OR `decided_at`, although 0020's own verification reported that protection as found, fixed and re-tested. Two tests were recorded as passing against a real authenticated profile — "decided_by cannot be rewritten to another editor once accepted — rejected" and "decided_at cannot be shifted — rejected". Neither guard was ever in the function. Confirmed on 2026-08-29 by `pg_get_functiondef` against the live `marketprices-rebuild` database: the applied body freezes `scope`, `key`, `current_value`, `proposed_value`, `evidence`, `sample_size` and `proposed_at`, and nothing else; `decided_by` and `decided_at` appear nowhere in it. The live definition is byte-identical to the committed file, and `0020_measurement.sql` has exactly one commit (5fbe1a5) in which those two columns appear only in table DDL, CHECK constraints and comments — so this is not a protection that existed and was later lost, it is one that was never written. THE EXPOSURE: on any row whose status is no longer `proposed`, an authenticated editor may reassign authorship of a finalised decision to a different editor and move its timestamp. The `status` branch does not catch it, because it sits inside `if new.status is distinct from old.status` and such an update leaves status alone. The CHECK constraints narrow without closing — `weight_proposals_check` stops `decided_by` being nulled on an accepted row and `weight_proposals_check2` stops `decided_at` moving earlier than `proposed_at`, but swapping `decided_by` to another valid profile and shifting `decided_at` forward are both unconstrained — and `weight_proposals_update_decide` is `is_admin_or_editor(auth.uid())` on USING and WITH CHECK with no status predicate, so it does not close it either. P16.5 puts a named person at the end of every weight change, which makes `decided_by` the whole of that accountability; a record any editor can quietly reassign after the fact is not a record. WHY THE ORIGINAL TESTS REPORTED A FALSE PASS, and this is the part worth carrying past this entry: `profiles` holds zero rows and so does `auth.users`, so `is_admin_or_editor()` returns false for every uid, and under RLS a denied UPDATE does not raise — it silently matches ZERO ROWS. A test concluding "rejected" from the observation that the value did not change therefore reports a pass on a statement the trigger never saw. The guard was not weak; it was never exercised. The lesson is method rather than SQL: a negative database test must assert the SPECIFIC error it expects, and must run beside a positive control proving the guard is engaged at all — observing that a value stayed the same cannot distinguish "the trigger refused this" from "the policy matched nothing" from "the statement never ran". VERIFIED THE OTHER WAY on 2026-08-29 before 0023 was written: in a transaction rolled back by a terminal `raise`, with two real `editor` profiles inserted and `auth.uid()` confirmed resolving, rewriting `decided_by` to the other editor and shifting `decided_at` five days forward both succeeded (`rows=1`, no error), while the controls — editing `proposed_value` and re-deciding `status` — both raised, proving the trigger was live on those same statements. 0020 is applied and is not edited; the fix is `0023_weight_proposal_decision_guard.sql`, a `CREATE OR REPLACE` freezing both columns once `old.status <> 'proposed'`, leaving the existing trigger and the service-role early return untouched. Numbered 0023 so 0022 stays with the functions batch.
- [Functions] FOUR APPLIED MIGRATIONS CARRY A COMMENT THAT 0022 MAKES FALSE, and none of them can be edited. `0013_articles.sql` (~line 182), `0016_videos.sql` (~176), `0017_engine_sources.sql` (~165) and `0018_content_items.sql` (~85) each assert, in the comment sitting directly above their `updated_at` (or `last_synced_at`) column, that no `set_updated_at` trigger exists anywhere in this build yet and that whatever writes the row therefore sets the column by hand. All four were true when written and all four are false the moment `0022_functions.sql` applies. Same disposition as 0021's numbering note: an applied migration is never edited to correct a comment in place, so this entry is the durable record. Read those four comments as dated statements about the build at the time of writing, not as current fact. Worth noting that 0013's version is not merely stale but is the SANCTION for 0022 — it continues "If that ever becomes a trigger it should become one for every table at once, not for this one alone", which is precisely the twelve-table shape 0022 ships and is quoted in that migration's header.
- [Functions] `0022_functions.sql` IS A PARTIAL FUNCTIONS BATCH, deliberately. The build plan's migration table (`marketprices-build-plan.md` ~line 728) defines the batch as four functions — `iso_week_of()`, `zscore_12wk()`, `basket_cost()` and `set_updated_at()`. 0022 ships the last one only; the other three are deferred to Stage 4. Verified against `pg_proc` on 2026-08-28 that none of the three exists yet, so nothing depends on them. The reason is testability rather than effort: all three are price/analytics functions with no current consumers and no job to exercise them against, and a function shipped with no caller is a function whose first real test happens in production. `iso_week_of()` additionally must not be written before it is reconciled with `lib/weeks.ts`, which is the stated single source of ISO week arithmetic on the application side — two independent implementations of the same week boundaries is the failure this project can least afford. They land as their own migration when Stage 4 gives them callers. (The plan names this file `0020_functions.sql`; disk 0020 is measurement and 0021 governance — the same systematic +2 offset recorded under [Engine] above.)
- [Functions] `videos.last_synced_at` IS DELIBERATELY EXCLUDED from 0022's triggers, against the apparent invitation in 0016 (~176), which anticipates this batch by name: "no table in this build has a set_updated_at trigger yet — so the cron sets it explicitly in the upsert's DO UPDATE". The invitation is declined because the column is not an `updated_at` under another name. It means "when the sync last saw this row", and it exists to distinguish a current mirror from the last thing a job that has been failing for a week managed to write — P2.9's staleness problem in a different table. A generic BEFORE UPDATE trigger would also stamp it on any editorial update to a `videos` row, asserting a sync that never ran and destroying the exact signal the column carries. It stays written explicitly by the sync, which is the only writer that knows whether a sync happened. Revisit only if `videos` ever grows a real `updated_at` alongside it, in which case that column joins the twelve and `last_synced_at` still does not.
- [Governance] SECURITY FINDING, TWO BUGS IN `0021_governance.sql`, FIXED FORWARD BY 0024 — both are the same mistake made twice: a SECURITY DEFINER function that takes the actor's identity as a caller-supplied argument and then gates on that argument. (1) `set_editorial_rule(p_scope, p_key, p_value, p_actor_id uuid default auth.uid())` checks `is_admin_or_editor(p_actor_id)`, writes `editorial_rules.changed_by = p_actor_id`, and passes the same value to the audit entry. The `default auth.uid()` reads as though identity comes from the session, and it does — only when the argument is omitted. Any caller may supply it instead. A signed-in contributor or analyst who knows one editor's uuid calls the function with it, passes the gate, and both the setting's `changed_by` and the `editorial_rule.set` audit row name an editor who did nothing. Confirmed against the live `marketprices-rebuild` database on 2026-08-31: the applied signature is still the four-argument form, `prosecdef` is true, and `proacl` reads `{=X/postgres,postgres=X/postgres,anon=X/postgres,authenticated=X/postgres,service_role=X/postgres}`, so every signed-in user can reach it. NOT EXPLOITABLE TODAY, and the reason is not a control: `profiles` is staff-only SELECT, `auth.users` is unreadable through the client, and both hold zero rows, so there is no staff uuid to borrow. THE DAY THAT CHANGES IS THE DAY THE FIRST ARTICLE PUBLISHES — `articles_select_public` grants `anon, authenticated` SELECT on every `status = 'published'` row (0013_articles.sql ~line 310), and those rows carry `author_id` (not null) and `published_by`, both foreign keys to `profiles(id)`: exactly the uuids `is_admin_or_editor()` accepts. The impersonation argument becomes public data on the first publish, on a schedule this repo does not choose. (2) `write_audit_entry(p_actor_id, p_action, p_entity_type, p_entity_id, p_diff)` is the same shape with a wider blast radius. It is SECURITY DEFINER, carries the same PUBLIC + `anon` + `authenticated` + `service_role` EXECUTE grants, and its only gate is `if p_actor_id is not null and not is_staff(p_actor_id)` — so a null actor is explicitly accepted as "the system". Any caller can therefore write any audit row: an action that never happened, attributed to a real staff member, or attributed to the system itself. 0021's comment calls this function "the only door into `audit_log`" and treats that as the security property. It is not. The table REVOKE and `protect_audit_log()` answer one question — can a row be altered or removed once written — and they answer it well. Nothing in the migration answers the other one: may this caller write this row at all. IMMUTABILITY IS NOT WRITE-AUTHORISATION, and the two are easy to conflate precisely because the tamper-proofing is thorough; append-only makes a forged entry permanent, which makes the weaker gate the one that matters more. P1.5 wants a record of what staff did, and a log anyone can write to is not that record, any more than a decision record any editor can reassign is one (the 0023 entry above; same failure, different table). THE FIX (0024, agreed and not yet applied at the time of this entry; `create or replace` plus explicit revokes, because 0021 is applied and applied migrations are not edited): identity stops being a parameter. `set_editorial_rule()` drops `p_actor_id` and reads `auth.uid()` in its body, so the value it gates on and the value it records are the same value and neither comes from the caller — the signature goes from four arguments to three. `write_audit_entry()` keeps its actor parameter, because it logs on behalf of other actions and must still be able to name a system write, and instead stops being reachable from the client: EXECUTE revoked from `anon` and `authenticated`, leaving it callable from inside the SECURITY DEFINER functions that log (those run as owner) and from `service_role`. THE `authenticated` EXECUTE GRANT ON `set_editorial_rule()` IS DELIBERATELY KEPT — it is the Settings screen's only path in, and once identity comes from `auth.uid()` the question of who may call the function no longer decides who the function can act as. Restricting the caller would be defence in depth against a hole already closed, at the cost of the one route the application has. VERIFIED 2026-08-31 that the signature change strands nothing: no `.rpc(` call exists anywhere in the repo, `lib/queries/` does not exist yet, and no document names the argument list — the only other occurrence of the four-argument form is generated `types/database.ts`, regenerated after the migration applies.
- [Governance] `REVOKE ... FROM PUBLIC` DOES NOT LOCK A FUNCTION IN THIS DATABASE — the first draft of the 0024 fix above got this wrong, and it is recorded separately because it will look correct in every future diff. Supabase's default privileges grant EXECUTE on new functions in `public` to `anon`, `authenticated` and `service_role` BY NAME, on top of the implicit PUBLIC grant. Read back from `pg_proc` on 2026-08-31, every function in this build carries the same ACL — `{=X/postgres,postgres=X/postgres,anon=X/postgres,authenticated=X/postgres,service_role=X/postgres}` — where `=X/postgres` is the PUBLIC grant and the three named roles hold grants of their own that removing PUBLIC does not touch. So `revoke execute on function f(...) from public;` removes one grant of four and leaves the function exactly as callable as it was, while reading in review like a door being shut. Every revoke in this build must name the roles it means — `revoke execute on function f(...) from public, anon, authenticated;` — and must be confirmed by reading `proacl` back after the migration applies, not by reading the migration. The general form is the part worth carrying past this entry: a privilege change is verified only when the privilege is read back, because a REVOKE naming the wrong grantee fails silently and is indistinguishable, in the diff, from one that worked.
- [Content items] `0018_content_items.sql`'s CONTRACTS ITEM 2 IS SUPERSEDED BY 0026, NOT IMPLEMENTED BY IT, and the applied migration is not edited to say so. 0018 defers the cross-table consistency check with a printed contract — "A row exists in format_overrides if and only if format_overridden is true, and override_reason mirrors the latest row's reason. Cross-table, so it belongs with the triggers batch." That sentence cannot hold. The log is append-only, its FK is ON DELETE RESTRICT, and 0018's own `check (recommended <> chosen)` forbids the only row that could record a withdrawal, so under the printed `if and only if` the first override sets `format_overridden` true FOREVER: there is no row to delete and no row that can say the override was retracted. The flag would come to mean "was ever overridden" rather than "is overridden", while Draft studio's filter reads it as the latter — and 0018 itself contemplates the case, saying format "is chosen at promote and the item then sits in Draft studio where it can be reconsidered". THE INVARIANT 0026 ENFORCES INSTEAD: an item's format decisions are an alternating sequence of overrides and withdrawals beginning unoverridden; `format_overridden` is true exactly when overrides outnumber withdrawals (which they do by exactly one while one is in force); `override_reason` is the reason of the override currently in force, or null when none is; and `format` and `recommended_format` must agree with the latest logged decision at commit. ALTERNATION IS NOT AN EXTRA RULE — with `recommended` fixed for an item and only two formats, "override" means chosen = the other one and "withdraw" means chosen = recommended, so a second override with no withdrawal between records nothing the first did not. This refines 0018's note that "under a second override this table holds two rows": it does, but only across a withdrawal. Read 0018's contract and the comment above its dropped check as dated statements; this entry is the durable record.
- [Content items] `format_overrides`'s `check (recommended <> chosen)` IS DROPPED BY 0026 and the rule moves into `apply_format_override()`, which enforces it MORE tightly than the check ever did. 0018's reasoning for the check is sound and is preserved: "A row where these match is not an override — it is a record of agreeing with the recommendation, which is the default and needs no row. Storing one would corrupt the retuning signal this table exists to produce, by counting a concurrence as a correction." That is true of a concurrence AT PROMOTE and false of a WITHDRAWAL, which is not agreement with the recommendation but the retraction of a disagreement — and P14.7 ("an override you do not record is an experiment you ran and threw away") applies with more force to the withdrawal than to the override, because the withdrawal is that experiment's RESULT and is the most informative row a retuning could read. The permission could not stay a CHECK because it is conditional on OTHER ROWS: an equal-valued row is legal only when an override is currently in force for that item, and a CHECK constraint cannot see another row. The trigger also closes what the check left open — the check permitted an unlimited run of identical overrides; the trigger permits none.
- [Content items] SECURITY FINDING, FIXED FORWARD BY 0026, TWO ISSUES ON `format_overrides` FOUND WHILE BUILDING THE CONSISTENCY CHECK. (1) THE PERMISSION MISMATCH, which failed SILENTLY rather than loudly. `format_overrides_insert_promote` admits ANY `can_promote()` user against ANY item — `with check (can_promote(auth.uid()))`, no ownership test — while `content_items_update_staff` admits only `is_admin_or_editor(auth.uid()) OR (can_promote(auth.uid()) AND created_by = auth.uid())`. So a Contributor logging an override on an item somebody else created passes the first gate and fails the second, and under invoker rights that failure is a ZERO-ROW UPDATE: no error, no flag set, a log row asserting a decision that was never applied, and nothing anywhere reporting it. Note that SECURITY DEFINER alone would have closed this the WRONG WAY, by letting the write through on the owner's privileges and quietly widening who may edit whose items; `apply_format_override()` is definer so the check is REACHED, and then re-checks `content_items_update_staff` explicitly and raises. Verified 2026-09-01 that the Contributor case now fails with that specific message and leaves no log row behind. (2) IDENTITY IS A CALLER-FILLED COLUMN — `format_overrides.actor` is `not null references profiles(id)` and no policy ever ties it to the session, so any promoter could log an override in another staff member's name. This is 0024's bug in column form rather than argument form, on the one table whose entire purpose is attributed evidence (0018: "a row with a null actor cannot inform a retuning, because 'somebody overrode this' is not evidence about anybody's judgement"). 0026 requires `actor = auth.uid()` and requires `auth.uid()` to exist; a format choice is a human act (§8.11) so there is no system actor to accommodate, the same posture 0024 took on `set_editorial_rule()`.
- [Content items] `format_overrides` WAS APPEND-ONLY BY POLICY ONLY, AND 0026 MAKES IT APPEND-ONLY IN FACT — the same hole 0025 closed on `price_observations`, found the same way. 0018 gives the table a SELECT and an INSERT policy and no UPDATE or DELETE policy, and its comments treat that as the append-only guarantee ("An append-only log of decisions actually taken; editing one would rewrite the evidence the heuristics are retuned against"). Read from the live catalogue on 2026-09-01, `relacl` was the untouched default — `{postgres=arwdDxtm/postgres, anon=arwdDxtm/postgres, authenticated=arwdDxtm/postgres, service_role=arwdDxtm/postgres}` — so that posture held for a policy-bound session and not at all for `service_role` or psql. This is load-bearing for 0026 rather than adjacent tidying: all three consistency layers DERIVE the truth from this log, so a mutable log does not merely lose history, it makes the derived flag forgeable, because editing the log edits what every guard believes. `service_role` also loses INSERT, per the human-act reading above; no current job writes here, but a future one would have to be a SECURITY DEFINER function rather than a service-role INSERT. Read back after applying: `{postgres=arwdDxtm/postgres, anon=rxtm/postgres, authenticated=arxtm/postgres, service_role=rxtm/postgres}`.
- [Content items] REVOKING `EXECUTE` ON A TRIGGER FUNCTION DOES NOT STOP THE TRIGGER FIRING, and the first draft of 0026 got the consequence of that backwards. The draft left its three trigger functions with their default grants, reasoning that a PL/pgSQL trigger function refuses to run outside trigger context so the grant conveys nothing to call. The premise is true and the conclusion still does not follow, though for a narrower reason than 0026's header claims. That header says "PostgREST exposes every function in `public` that a role may execute", which is too broad: PostgREST filters candidate functions by return type, and whether a `returns trigger` function is actually reachable at `/rest/v1/rpc/` was NOT verified in that session. What IS established is that Supabase's own linter flags all three under `0028_anon_security_definer_function_executable` and `0029_authenticated_...`, asserting the REST path in its message, and that the grant itself conveys nothing any caller could use. So the revoke is right on the weaker ground — it removes a grant with no purpose and clears a standing WARN whose harmlessness a reviewer would otherwise have to re-derive on every pass — rather than on the stronger ground of closing a live endpoint. Treat 0026's sentence as the overstatement it is; correcting it in place is not available, since that migration is applied. All four of 0026's functions are therefore revoked from `public, anon, authenticated, service_role`. THE GENERAL FACT WORTH CARRYING PAST THIS ENTRY: `EXECUTE` on a trigger function is checked when the trigger is CREATED, not each time it fires, so a trigger whose function is revoked from every role still runs for every role — verified 2026-09-01 by revoking all three and re-running the full behavioural suite, which returned identical results across all 34 assertions.
- [Content items] THE APP MUST NOW MOVE `format` AND `archetype` IN ONE STATEMENT, and an override is three statements in one transaction rather than one write. 0026's guards make this a hard requirement rather than a convention, so it is recorded outside the migration too. (1) Insert the `content_items` row at `format = recommended_format` with an archetype legal for that format — an item cannot be born overridden, because the `format_overrides` row recording the decision cannot exist before the item it points at, and the BEFORE INSERT guard rejects it. (2) UPDATE `format` AND `archetype` TOGETHER: `content_items`' archetype check is a table CHECK and therefore immediate (ten prose skeletons legal only for `article`, V1–V6 only for `video`), so a statement moving either alone violates it. This is also why the log trigger does NOT set `format` itself — a trigger moving format without archetype would fail that check on every override, with an error naming the archetype rather than the cause. (3) Insert the `format_overrides` row; the trigger applies the flag and reason. Steps 2 and 3 may be given in either order — the trigger deliberately does not compare `chosen` against the item's current format, and the deferred assertion makes them agree at commit either way. A WITHDRAWAL is the same shape: move format and archetype back, log a row with `chosen = recommended` and a reason. The withdrawal's reason lives in the log and is deliberately NOT copied onto the item, because `override_reason` describes the override in force and after a withdrawal there is none. ONE DECISION PER TRANSACTION PER ITEM: `created_at` defaults to `now()`, which is transaction start, so two rows written in one transaction share a timestamp and "the latest row" stops being a question the data can answer — but the rule is editorial before it is technical, since an override and its withdrawal in one transaction is not two decisions, it is a change of mind that never reached the product, and recording it would put a correction into the retuning signal that no editor ever acted on.
- [Content items] OVERRIDES ARE UNAVAILABLE UNTIL THE FORMAT-FIT GAP CLOSES, a deliberate consequence of 0026 and not a defect. `apply_format_override()` refuses to log a decision against an item whose `recommended_format` is null, on the ground that an override of nothing is not an override. Because format-fit scores still have no source (the NOTED GAP above, which also blocks the two remaining promote functions), `recommended_format` is null on every item today, so no override can be logged at all until that question is settled. The honest posture is that the feature waits on the recommendation it exists to correct. 0026 also forecloses moving `recommended_format` after a decision has been logged against it — a recommendation that shifts under a recorded correction makes the correction unreadable — which makes 0018's freeze of the format-fit figures structural from the first logged decision onward, and means a future backfill of `recommended_format` onto an existing item is refused unless it agrees with the item's current format.
- [Content items] 0026 WAS APPLIED THROUGH THE MCP `apply_migration` TOOL, WHICH RECORDED IT UNDER A TIMESTAMP VERSION (`20260901090241`) RATHER THAN `0026`, diverging from `0021`–`0025`, which are recorded under their plain repo numbers. Left as-is because correcting `supabase_migrations.schema_migrations` is a write to the CLI's own ledger and was not authorised in the session that applied it. CONSEQUENCE, AND IT IS NOT COSMETIC: `supabase db push` matches local files to that table BY VERSION, so with the row reading `20260901090241` the CLI sees `0026_format_override_consistency.sql` as unapplied and will try to run it again, where it will fail on `create function` and on the already-dropped `format_overrides_check`. Fix before the next push, by updating that row's `version` to `0026` (and appending the four `revoke execute` statements, which were applied separately after the advisor pass and are in the repo file but not in the recorded statement list). The three `revoke`s on the trigger functions and the one on `format_override_state` are the only drift between the file and the ledger; the schema itself matches the file exactly, verified by reading `pg_proc.proacl`, `pg_constraint` and `pg_trigger` back.
- [Governance] `can_view_audit_log()`'s SEARCH_PATH PIN, PROMISED BY NAME IN 0024 AND TAKEN IN 0027. 0024's header states the debt explicitly — "Of the sixteen SECURITY DEFINER functions in this schema, the only three with no pinned search_path are 0021's. Two of them are rewritten here and get the pin; `can_view_audit_log()` is untouched by this migration and still needs it, in its own follow-up." Confirmed against `pg_proc` on 2026-09-01 that it was still the only SECURITY DEFINER function in `public` with `proconfig` null, that it is `STABLE sql`, that it reads `profiles` unqualified, and that it backs exactly one policy (`audit_log_select_governance` on `audit_log`, called with no argument so it uses its `default auth.uid()`). Applied as `alter function ... set search_path = public` rather than `create or replace`: only the setting changes, so there is no reason to restate the body and no risk of restating it wrongly, and no dependency moves. ITS EXECUTE REVOKE IS DELIBERATELY PARTIAL — `public, service_role` only — AND THIS IS THE ONE FUNCTION IN EITHER MIGRATION WHERE 0026'S FOUR-ROLE SHAPE CANNOT BE USED. It is the only function touched by 0026 or 0027 that is REFERENCED BY AN RLS POLICY (`audit_log_select_governance`, whose USING expression is `can_view_audit_log()`), and a policy expression is evaluated with the QUERYING USER'S privileges, so a caller without EXECUTE does not get "no rows", they get an error and the table becomes unreadable. Verified in the rolled-back transaction: with EXECUTE revoked from `authenticated`, an analyst selecting from `audit_log` failed with `permission denied for function can_view_audit_log`, where the same query had returned its row moments earlier — so a four-role revoke would take the governance audit view down for every member of staff meant to see it. Same call 0024 made for `set_editorial_rule()` ("it is the Settings screen's only path in"), reached here for a stronger reason: there the grant was the application's route, here it is load-bearing for a policy. `anon` KEEPS EXECUTE TOO, for a subtler reason: `anon` holds table-level SELECT on `audit_log` (`relacl` reads `anon=rxtm/postgres`) and the policy applies to PUBLIC rather than a named role, so an anonymous SELECT DOES evaluate this function today — `auth.uid()` is null, it returns false, and the caller gets a clean empty result. Revoking `anon` would convert that empty result into `permission denied for function can_view_audit_log`, which tells an anonymous prober MORE than the empty set did, not less. The quieter failure is the better one. NOT FIXED, AND A DELIBERATE ASYMMETRY WITH 0024: the function still takes `uid` as a caller-supplied argument with an `auth.uid()` default, which is the shape 0024 removed from `set_editorial_rule()`. It is benign here because the function only reads and returns a boolean and its only caller passes nothing — the worst case is an oracle for "does this uuid hold a governance role", not impersonation — and changing the signature would mean dropping and recreating a function a live policy depends on. Recorded so the asymmetry is a decision on the record rather than an oversight.
- [Governance] SECURITY FINDING, FIXED FORWARD BY 0027 — `protect_profile_privileges()` (0003_profiles.sql) COULD BE SKIPPED BY SEARCH_PATH RESOLUTION, and it is the one place this build's stated reason for leaving the plain trigger functions unpinned does not hold. 0022 and 0025 both justify the omission with the same sentence: "Not SECURITY DEFINER and no search_path pin: the body touches no table, no schema-qualified object and no other function — only NEW and now()". Checked against all seven bodies on 2026-09-01, that is true of six — `set_updated_at` (NEW and `now()`), `protect_audit_log` (a bare raise on `tg_op`), `protect_article_provenance` (NEW/OLD only), and `protect_content_status`, `protect_weight_proposal`, `protect_price_observation` (NEW/OLD, jsonb operators, and a schema-qualified `auth.uid()`). It is not true of `protect_profile_privileges`, whose first line reads `if new.id = auth.uid() and not is_admin(auth.uid()) then` — `is_admin` UNQUALIFIED, in a SECURITY INVOKER function with no pin, so the NAME resolves through the CALLER's search_path at execution time. That `is_admin()` is itself SECURITY DEFINER with a pinned search_path does not help in the slightest: the pin governs what happens inside it once chosen, not which function of that name gets chosen. THE MECHANISM WAS DEMONSTRATED, NOT ASSUMED — in the rolled-back verification transaction a `decoy` schema holding `is_admin(uuid)` as `select true` was put ahead of `public` on an ordinary contributor's search_path, and `update profiles set role='admin' where id=<self>` SUCCEEDED, with the row coming back reading `role = admin`; against the replacement the identical attempt was refused and the row stayed `contributor`. `profiles_update_own` already grants that user the UPDATE, so the trigger was the only thing standing between a signed-in contributor and self-promotion to admin. WHAT MAKES IT THEORETICAL IS THE PRECONDITION, NOT THE MECHANISM: `authenticated` cannot CREATE SCHEMA in a stock Supabase project, so today there is nowhere to put the decoy. Fixed anyway because the blast radius is the highest-value write in the schema, because the precondition is a GRANT rather than a code change and could be restored by an unrelated future decision to let some role create objects with nothing connecting that decision back to this function, and because it is one line. THE FIX IS BOTH HALVES — the pin fixes resolution for every unqualified name in the body including any added later, and the qualification makes the call site say what it means so the guarantee survives someone removing the pin without reading the header. It stays SECURITY INVOKER: it must read `auth.uid()` as the caller.
- [Governance] 0027's SIX REVOKES ON THE SECURITY INVOKER TRIGGER FUNCTIONS ARE CONSISTENCY, NOT A SECURITY FIX, and the migration says so in its own header so that nobody later cites it as the latter. `protect_article_provenance`, `protect_audit_log`, `protect_content_status`, `protect_price_observation`, `protect_weight_proposal` and `set_updated_at` all run with the CALLER's privileges, PL/pgSQL refuses to execute a trigger function outside trigger context, and Supabase's SECURITY DEFINER lints (0028/0029) do not flag any of them — only the definer-marked ones, which is why 0026's three and 0027's `handle_new_user_entitlement()` are in a different category. Nothing among the six was exploitable and nothing becomes safe that was not. They are revoked so that a reviewer can read "trigger function" as implying "not callable" without checking each one. SEVEN INVOKER TRIGGER FUNCTIONS EXIST AND ONLY SIX ARE IN THAT GROUP: `protect_profile_privileges` is the seventh and is revoked alongside its own fix instead, with `service_role` included, because it is a real fix rather than a consistency pass. THE REVOKE POSTURE ACROSS 0026 AND 0027, stated once so it is not re-derived: the rule both express is that EVERY FUNCTION IS REVOKED FROM EVERY GRANTEE THAT DOES NOT NEED IT. That yields `{postgres=X/postgres}` for 0026's four and for 0027's `handle_new_user_entitlement` and `protect_profile_privileges`; `{postgres, anon, authenticated}` for `can_view_audit_log`, which needs both client roles for the reason in the entry above; and `{postgres, service_role}` for the six consistency revokes, where `service_role` is kept because it is a trusted server-side identity that already bypasses RLS everywhere and the functions are inert to every caller, so a fourth statement per function buys nothing. If a later pass wants the six to match the rest, it should revoke `service_role` from all six at once rather than introducing a fourth shape.
- [Measurement] 0030 NARROWS THE FOUR RETENTION ANSWERS ABOVE TO ONE, a deliberate consequence rather than a defect, recorded separately from the entry above because it changes that open item's cost rather than resolving it. `0030_append_only_locks.sql` gives `site_events` the three-layer append-only lock 0020 described in prose and never built ("No update or delete policy, ever. A log is append-only or it is not a log."): Layer 2 revokes `update, delete, truncate` from `anon`, `authenticated` AND `service_role`, and Layer 3 — `protect_site_event()`, wired as `before update or delete ... for each row` plus `before truncate ... for each statement` — raises unconditionally, with no `auth.uid() is null` escape, because on this table the bypassing role is the threat rather than a confused human. Of the four answers the entry above lists, three now require a migration before they can be attempted at all: DROP AFTER N DAYS and ROLL UP THEN TRUNCATE are blocked outright, and PARTITION BY MONTH is blocked at the point where an old partition is detached and dropped. KEEP EVERY ROW FOREVER is the only strategy compatible with the lock as applied, and it is therefore the de facto retention policy from 0030 onward — not because it was chosen on the merits, but because it is the only one requiring no schema change. WHY A CRON JOB CANNOT FIX THIS LATER, which is the trap this entry exists to disarm: a retention job would naturally be written as a SECURITY DEFINER function, by analogy with `supersede_price_observation()` (0025) and `set_editorial_rule()` (0021), both of which pass their table's REVOKE by running as owner. That analogy fails here. SECURITY DEFINER escapes a REVOKE; it does NOT escape a TRIGGER — triggers fire for the owner and for superusers alike and there is no definer exemption, which 0025 states for `price_observations` and is the same mechanism. So a retention job that deletes `site_events` rows cannot be added at all until `protect_site_event()` is REPLACED with a carve-out shaped like the retention rule itself — predicate-shaped, e.g. permitting `DELETE` only where `created_at < now() - <interval>`, in the manner of `protect_price_observation()`'s supersede carve-out — and that replacement is a reviewed migration, not a job somebody schedules. THIS IS THE INTENDED SEQUENCE, not an oversight: the log is sealed until a retention policy exists, and the policy then arrives as a reviewed change to the guard rather than as an unreviewed capability that was sitting there unused all along. The cost is one migration at the moment retention is settled; the benefit is that between now and then there is no path — service-role, owner or otherwise — by which measurement data can be quietly discarded, which is what P11.1 requires if "metrics are real or absent" is to constrain the past as well as the present. The urgency note in the entry above is unchanged and still governs: settle retention before the table has a year of traffic in it, and settle retention BEFORE partitioning.
- [Governance] THE TRUNCATE GAP IS A PROPERTY OF THIS DATABASE'S DEFAULT PRIVILEGES, NOT OF THE THREE TABLES 0031 FIXES, and it therefore REOPENS SILENTLY ON EVERY FUTURE `create table` unless that migration closes it by hand. Recorded because 0031's header explains the gap only for `basket_snapshots`, `daily_rollups` and `content_performance`, which makes it look like a three-table oversight when it is a database-wide default. THE MECHANISM, read from `pg_default_acl` on 2026-09-01: schema `public`, object type `r`, carries TWO default-ACL entries — one granted by `postgres` and one by `supabase_admin` — and both read `{postgres=arwdDxtm/postgres, anon=arwdDxtm/postgres, authenticated=arwdDxtm/postgres, service_role=arwdDxtm/postgres}`. Every privilege, including `D` (TRUNCATE), to every Supabase role, on every table created in `public`. Verified by execution rather than inference: `create table _future_table_probe (id int)` inside a rolled-back transaction came into existence with exactly that ACL, `relrowsecurity` FALSE, and `has_table_privilege('anon', ..., 'TRUNCATE')` already true. A brand-new table is fully writable and fully truncatable by anonymous callers from the moment it exists. WHY THE BUILD'S EXISTING DISCIPLINE DOES NOT CATCH IT: every table in this schema follows the same habit — `alter table ... enable row level security`, then SELECT-only (or narrowly scoped) policies — and that habit genuinely closes INSERT, UPDATE and DELETE, because RLS default-denies any command with no matching policy. It does NOT close TRUNCATE, because RLS DOES NOT APPLY TO TRUNCATE AT ALL: TRUNCATE is governed by the table privilege alone and is never filtered by a policy. So the one command RLS cannot reach is also the most destructive, and the review habit that has protected every other command is structurally blind to it. THE MEASURED STATE on 2026-09-01, after 0030 and before 0031: 25 of the 31 tables in `public` can be TRUNCATEd by `anon` or `authenticated`, and the ONLY six that cannot are precisely the six an explicit REVOKE has reached — `audit_log` (0021), `price_observations` (0025), `format_overrides` (0026), and `content_revisions`, `editorial_rules`, `site_events` (0030). Nothing else in the build has ever narrowed a table grant, so the correlation is exact: protection here has come only from migrations that asked for it by name. THE STRUCTURAL FIX AND WHY IT IS NOT FREE: `alter default privileges in schema public revoke truncate on tables from anon, authenticated` changes what future tables are born with, and it is the only thing that stops the gap reappearing. Three caveats belong with it. (1) ALTER DEFAULT PRIVILEGES is PER GRANTOR ROLE — it only governs objects created by the role that issued it — and there are two grantor entries here (`postgres`, which is what migrations run as, and `supabase_admin`), so closing only the obvious one leaves the other open. (2) It affects FUTURE objects ONLY: the 25 tables above keep their grants and still need a one-time explicit sweep, which is a separate migration and a much larger blast radius than 0031's three tables. (3) Supabase platform operations manage these defaults, so this is not a set-and-forget change — it should be re-verified against `pg_default_acl` after any platform-level change rather than assumed permanent. UNTIL THAT LANDS, THE STANDING REQUIREMENT: every migration that creates a table in `public` must revoke the privileges that table's writers do not need, in the same migration that creates it, and enabling RLS is NOT a substitute for that. Adding the table and its policies and trusting the policy set is exactly the pattern that produced the 25.
- [Governance] THE TRUNCATE GAP ABOVE IS CLOSED BY 0032, WITH ONE RESIDUAL THAT NO MIGRATION CAN REACH. `0032_truncate_revocation_sweep.sql` is the fix the entry above calls for, in three sections: (1) `alter default privileges in schema public revoke truncate on tables from anon, authenticated`, which governs every table created by `postgres` from that point on; (2) a single atomic `revoke truncate` naming the tables that already existed; and (3) a closing assertion. THE COUNT MOVED BETWEEN THE TWO ENTRIES AND THE EARLIER NUMBER IS NOT WRONG, IT IS STALE: the entry above says 25 of 31 tables were exposed, which was true when it was written; `0031_derived_table_write_revokes.sql` was applied in between and took `basket_snapshots`, `daily_rollups` and `content_performance` out, so 0032 was drafted against the 22 that remained — `article_tags`, `articles`, `basket_definition`, `collection_sites`, `collectors`, `commodities`, `content_items`, `content_templates`, `entitlements`, `homepage_pins`, `media`, `price_anomalies`, `price_submissions`, `profiles`, `raw_items`, `sections`, `signals`, `sources`, `tags`, `units`, `videos`, `weight_proposals`. Anyone reconciling the two numbers later should read the difference as three applied migrations, not as a miscount. THE UNREACHABLE RESIDUAL, and it is the reason this entry exists rather than a line in the migration alone: `pg_default_acl` carries TWO default-ACL entries for schema `public` object type `r`, one granted by `postgres` and one by `supabase_admin`, and 0032 can only change the first. Verified by execution in a rolled-back transaction on 2026-09-01, as `postgres`, which is what migrations run as: `alter default privileges in schema public revoke truncate ...` SUCCEEDED; `alter default privileges for role supabase_admin in schema public revoke truncate ...` FAILED with "permission denied to change default privileges"; and `set role supabase_admin` FAILED with "permission denied to set role \"supabase_admin\"". `postgres` is not a superuser (`rolsuper` false) and is not a member of `supabase_admin`, and `ALTER DEFAULT PRIVILEGES FOR ROLE x` requires membership in `x`, so there is no escalation path from any migration. Changing that half needs a superuser session — the dashboard SQL editor — or Supabase platform support, and it is deliberately NOT attempted in 0032 rather than being wrapped in an exception handler that would make a permanent limitation look like a transient failure. WHY THE RESIDUAL IS CURRENTLY HARMLESS, AND EXACTLY WHEN IT WOULD STOP BEING: default privileges are keyed to the role that CREATES an object, and all 31 tables in `public` are owned by `postgres` — every one created by a migration — so the `supabase_admin` default has never governed a single table in this project. It would govern one the moment Supabase's own tooling creates a table in `public` as `supabase_admin`, which is outside this repo's control and would arrive without review. That table would be born TRUNCATE-able by `anon` and `authenticated`, and nothing in this build would notice. 0032 IS TRUNCATE-ONLY AND THAT IS A BOUNDARY, NOT AN OVERSIGHT: no INSERT, UPDATE, DELETE or SELECT grant is touched and no policy is created, altered or dropped, because nineteen of the twenty-two tables carry live `authenticated` write policies (`article_tags`, `homepage_pins` and `tags` have DELETE policies; most of the rest have INSERT and UPDATE) and RLS needs the underlying grant for every one. Revoking those would not harden anything — it would break the admin surfaces while leaving the policies in place to suggest otherwise. `service_role` keeps TRUNCATE on all twenty-two, matching 0031. THE CLOSING ASSERTION AND WHAT IT MAKES TRUE: section 3 queries `pg_class` for the WHOLE schema rather than reading the twenty-two names, and raises if `anon` or `authenticated` still hold TRUNCATE on anything in `public`, covering `relkind` 'r' and 'p' so a partitioned table cannot slip through. A named list cannot know about a table created between drafting and applying; the assertion turns that from a silent miss into a named, loud failure that aborts the migration's transaction and leaves the schema untouched. This makes 0032 ORDER-SENSITIVE ON PURPOSE — if any table is created in `public` before it is applied, it will refuse to commit and name that table, and the correct response is to add the name to section 2, never to weaken the check. STILL NOT SET-AND-FORGET: Supabase platform operations manage these defaults, so `pg_default_acl` should be re-verified after any platform-level change rather than assumed to still read as it does today. The standing requirement in the entry above is unchanged and still governs every new table.
- [Anomalies/signals] TWO MIGRATIONS EACH CONTAIN TWO DIFFERENT COLUMN LISTS FOR THE SAME PARTITION, AND THE ONE NEXT TO THE POLICIES IS THE WRONG ONE. `0011_price_anomalies.sql` and `0017_engine_sources.sql` each state their machine/decision split twice: once as a formal contract near the end of the file, and once as a paraphrase in the comment block above the RLS policies. The two disagree, in both files, and the paraphrase is the one a reader meets first. TRUST THE CONTRACT, NOT THE PARAPHRASE. On `price_anomalies`: THE RECOMPUTE CONTRACT is authoritative and lists eight computed columns — `pct_change`, `z_score`, `baseline_expected`, `direction`, `severity`, `site_switch_flag`, `gap_weeks`, `detected_at`. The policy comment instead says the decision functions "cannot reach pct_change, z_score, severity, baseline_expected, gap_weeks, site_switch_flag or comparison_window" — which OMITS `direction` and `detected_at`, both genuinely machine columns, and ADDS `comparison_window`, which is not a machine column at all but part of the ledger key, as that same migration says explicitly two hundred lines earlier ("the comparison window is part of the identity of a rating, not an attribute of it"). On `signals`: THE RESCORE CONTRACT is authoritative and lists thirteen machine columns — `category`, `category_weight`, `geo_scope`, `geo_score`, `decision_utility`, `utility_breakdown`, `recency_score`, `novelty_score`, `commodity_match`, `signal_score`, `commodities`, `entities`, `scored_at`. The policy comment names six of them ("signal_score, decision_utility, utility_breakdown, category, category_weight or commodities"), omitting `geo_scope`, `geo_score`, `recency_score`, `novelty_score`, `commodity_match`, `entities` and `scored_at`. WHY THIS IS RECORDED RATHER THAN LEFT AS AN IMPRECISION: 0033 builds an enforced guard from these lists, and a future migration extending or revising that guard will go back to the same two files to find the column split. Building it from the paraphrase on `price_anomalies` would freeze `comparison_window` correctly by accident while leaving `direction` and `detected_at` unprotected against the decision path, and would leave seven of thirteen `signals` columns unconsidered. Both applied migrations are correct in what they ENFORCE — neither list is executable, both are prose — so there is nothing to fix forward and no migration to write; the lists are comments, and applied migrations are not edited. This entry is the correction. A THIRD CATEGORY EXISTS THAT NEITHER LIST MENTIONS, and it is the substantive finding rather than a documentation nit: both contracts are two-way (machine columns versus decision columns) and both are silent on the columns that are neither — `signals`' `id`, `raw_item_id` and `created_at`, and `price_anomalies`' `id` plus its entire ledger key (`commodity_id`, `tier`, `iso_year`, `iso_week`, `comparison_window`). A guard built literally from either contract would leave those writable by the very job it exists to constrain, which on `price_anomalies` is the most dangerous of the three cases: rewriting the ledger key does not corrupt a figure, it silently re-points a severity rating at a different commodity or a different week, and `price_anomalies_ledger_key` only notices on a collision. 0033 therefore enforces a THREE-way partition — identity frozen against every writer, decision block reachable only through a legal state transition, machine columns free to the job — and the third set is a departure from what either migration printed, recorded here so it reads as a deliberate extension rather than an invention.
- [Measurement] THE `protect_weight_proposal()` FINDING IS CLOSED OUT BY 0034, WHICH REMOVES THE HALF 0023 DELIBERATELY LEFT. The entry above records that 0023 froze `decided_by` and `decided_at` "leaving the existing trigger and the service-role early return untouched". That early return is the remaining half, and it is not a narrower version of the guard — it is the absence of one. `if auth.uid() is null then return new; end if;` is the FIRST statement in the function, so every rule beneath it (0020's frozen proposal body, 0023's frozen decision record, and the one-way finality of a decision) is skipped entirely whenever no signed-in person is behind the write. Confirmed still present on 2026-09-01 against the live `marketprices-rebuild` database before 0034 was drafted. THE EXPOSURE WAS DEMONSTRATED, NOT INFERRED, and deliberately using the method the entry above prescribes after that entry's own false pass: in a rolled-back transaction, under `set local role service_role` with `auth.uid()` asserted to be genuinely NULL (printed as part of the run, alongside `current_user = service_role`), five writes against a decided proposal ALL SUCCEEDED WITH NO ERROR — rewriting `proposed_value` on an accepted row, reassigning `decided_by` to a different editor, shifting `decided_at` five days forward, flipping `accepted` to `rejected`, and DELETING the decided proposal outright, which removed the row entirely. Beside them, as the positive control that the entry above insists on, the same two edits issued as a signed-in `editor` with `auth.uid()` resolving BOTH RAISED their specific 0020/0023 errors — proving the trigger was live and engaged on those very statements and that it is precisely the null-uid path that escapes it. WHY 0020 AND 0023 WERE BOTH REASONABLE AND ARE BOTH SUPERSEDED: 0020 introduced the escape with a stated rationale ("the guard constrains humans, because that is the direction the risk runs in") and 0023 inherited it explicitly. Both were defensible calls about a table with no reachable machine writer yet. Neither survives the standard adopted from 0025 onward and applied in 0021, 0030 and 0033, where the BYPASSING ROLE IS THE THREAT and "no identified user" is exactly the case that must still be judged. 0034 makes this the last guard in the build to become unconditional. WHAT 0034 ALSO ADDS: `weight_proposals` had NO DELETE OR TRUNCATE GUARD OF ANY KIND — the only trigger on the table was `weight_proposals_protect_decision`, BEFORE UPDATE — so a service-role or owner session could drop a proposal and its decision record outright, which the demonstration above did. 0034 adds `weight_proposals_no_delete` and `weight_proposals_no_truncate` and leaves the existing UPDATE trigger untouched, since CREATE OR REPLACE at the same signature means it picks up the new body without a live guard ever being dropped. A DISTINCTION WORTH RECORDING BECAUSE IT LOOKS LIKE A FAILURE AND IS NOT: after 0034, an `authenticated` DELETE against this table still raises NOTHING — it silently affects zero rows, because there is no DELETE policy and RLS filters every candidate row BEFORE the trigger is reached. The trigger's DELETE branch is what binds `service_role` and the table owner, and for those two it raises loudly. Verified all four paths separately: authenticated DELETE = 0 rows and no error; service_role DELETE = raises; owner DELETE = raises; owner TRUNCATE = raises; row survived all four. A test asserting "authenticated DELETE must raise" reports a false FAIL, for the mirror-image reason the entry above documents a false PASS. ONE GAP WAS LEFT OPEN BY 0034 AND IS CLOSED BY 0035: 0034 had no INSERT branch, so a row could still be INSERTED already `accepted` with a `decided_by` pointing at a real editor — `weight_proposals_check1` is satisfied by that shape — which, with DELETE refused, was the one remaining way to author a human decision without a human. It was outside 0034's specified scope and was recorded as a named gap rather than folded in silently; `0035_weight_proposal_born_open.sql` closes it, applying to this table the rule 0033 already applies to `signals` and `price_anomalies`: a proposal is born open, at status `proposed` with `decided_by` and `decided_at` both null. WHY 0035 IS A TRIGGER BRANCH AND NOT A SECOND CHECK CONSTRAINT, settled by experiment rather than preference and recorded because `weight_proposals_check1` makes a CHECK the obvious first instinct: a CHECK is a row-state invariant evaluated on every INSERT AND every UPDATE, with no way to tell them apart. The candidate `check (status = 'proposed' and decided_by is null and decided_at is null)` was added to the live table inside a rolled-back transaction on 2026-09-01 and exercised. It did reject the pre-accepted INSERT — and it also rejected THE LEGITIMATE ACCEPT TRANSITION and THE LEGITIMATE PROPOSER SUPERSEDE, both with the same constraint violation, because an accepted row cannot satisfy an invariant asserting every row is open; dropping the constraint and repeating the accept confirmed the transition itself was fine. A CHECK expressing "born open" therefore expresses "always open", which forbids the table's entire lifecycle. This is the same instrument distinction 0034's header draws — the CHECK says what a row may LOOK like, the guard says how a row may MOVE — and creation is a move, so it belongs with the other transitions in `protect_weight_proposal()`. 0035 adds the branch by CREATE OR REPLACE at the same signature and adds one trigger, `weight_proposals_insert_open`, leaving the three existing triggers in place. WITH 0035 APPLIED, every route to a decision record on this table is closed but the two real ones: the proposer may open a proposal and later supersede it, and a person may accept or reject it. A decision cannot be inserted, edited, reassigned, deleted or truncated away.
- [Engine] THE ENGINE'S DATABASE ROLE HAS NEVER BEEN CREATED, AND ITS IDENTITY MECHANISM WAS NEVER SPECIFIED — NOT DECIDED AGAINST, NOT DEFERRED, SIMPLY ABSENT. Recorded so this is not mistaken later for an open decision somebody already weighed. WHAT IS SETTLED: P5.2 (`docs/build-protocol.md` ~line 383, echoed at `marketprices-build-plan.md` ~752) fixes the PRIVILEGES exactly — "The engine's database role holds `SELECT` on `commodities`, `collection_sites`, `price_observations`, `price_anomalies` and `basket_snapshots`, and nothing at all on `price_submissions` or on writing to `price_observations`." WHAT IS NOT SETTLED, and is not addressed in any of the three specification documents: the role is never named, no migration is said to create it, and nothing anywhere describes how a deployed connection AUTHENTICATES as it. Note also that the role is specified in build-protocol.md and the build plan, NOT in `marketprices-design-and-system-architecture.md` — searched on 2026-09-01 — so a future reader looking for it in the architecture document will not find it. CONFIRMED NOT CREATED on 2026-09-01 against `pg_roles` in `marketprices-rebuild`: every role in the cluster is a Supabase built-in (`postgres`, `authenticator`, `anon`, `authenticated`, `service_role`, `supabase_admin`, `supabase_auth_admin`, `supabase_storage_admin`, `supabase_read_only_user`, `supabase_etl_admin`, `supabase_replication_admin`, `pgbouncer`, `dashboard_user`, `supabase_privileged_role`), with no `engine_read` or equivalent. THE `CREATE ROLE` STEP IS NOT THE COST. Verified end to end in a rolled-back transaction as `postgres`, which is what migrations run as: `create role engine_read nologin`, `grant engine_read to authenticator`, `grant usage on schema public`, and the five P5.2 `SELECT` grants all SUCCEEDED, requiring no privilege `postgres` lacks; acting as that role, `SELECT` on `price_submissions` and `INSERT` on `price_observations` were both refused with `permission denied`. `create role ... login password` also succeeded, so the direct-connection route is equally creatable. THE REAL COST IS RLS POLICY COMPATIBILITY, and it is easy to miss because it fails SILENTLY rather than loudly. A custom role does not bypass RLS (`rolbypassrls` false), and since `0029_policy_role_scoping_and_execute_revokes.sql` every SELECT policy on these tables is scoped to a named role list — `commodities_select_active` is `to {anon, authenticated}`, `basket_snapshots_select_public` likewise. A role outside those lists matches no policy and is default-denied. Demonstrated with real rows present rather than inferred from an empty table: with one `commodities` row and one `basket_snapshots` row inserted, `engine_read` holding an explicit table-level `SELECT` grant saw ZERO ROWS ON BOTH, WITH NO ERROR, while `anon` saw one row as the control, and adding a matching policy for the role made the same row appear. So a deployed engine would connect successfully, issue correct queries, receive an empty price series and never raise anything. Granting `SELECT` to a new role is necessary and not sufficient; every SELECT policy on the five P5.2 tables must also admit it, which reopens the role scoping 0029 deliberately tightened, and that — not `CREATE ROLE` — is the work. THE LEGACY JWT PATH IS CONFIRMED STILL AVAILABLE ON THIS PROJECT, checked on 2026-09-01: the project exposes both an enabled legacy `anon` key of type `legacy` (an HS256 JWT whose payload carries `"role":"anon"` alongside `iss`, `ref`, `iat`, `exp`) and a modern `sb_publishable_...` key, neither disabled. Because the legacy JWT path is still active, PostgREST will still accept a token signed with the project JWT secret carrying an arbitrary `role` claim, and `authenticator` — which is `NOINHERIT` and exists to `SET ROLE` into whatever the claim names — is the pivot that `grant engine_read to authenticator` provides. TWO CAVEATS ON THAT AVAILABILITY: minting such a token requires the project JWT SECRET, which is not the anon key and lives in dashboard settings; and the coexistence of a modern publishable key means the legacy path is a current fact rather than a permanent one, so it should be re-checked rather than assumed at the moment this is built. THE CHOICE BETWEEN THE TWO ROUTES SHOULD WAIT, and this is the actual recommendation: a JWT-based PostgREST connection and a direct Postgres connection (`LOGIN PASSWORD`, routed through Supavisor, secret in Vercel env and NEVER in a migration file, since a literal password in a migration is a secret committed to git) suit different runtimes, and the engine's runtime location — inside the Next.js app or as its own process — is itself undecided. That choice shapes which route makes sense, so deciding the connection mechanism first would be deciding it for a thing that does not exist. AND IT DOES NOT EXIST: as of 2026-09-01 `lib/` holds exactly three files (`cn.ts`, `constants.ts`, `cn.test.ts`), there is no `lib/queries/`, no `app/api/`, no `@supabase/supabase-js` and no Postgres driver in `package.json`; the repository's only database touchpoint is the `db:seed` script invoking `psql $DATABASE_URL`. Nothing in application code connects to this database yet. Revisit when the engine's runtime is chosen, and expect the migration to be small and the policy work to be the substance.
- [Auth] THE 12-HOUR SESSION TIMEOUT IS DEFERRED, NOT IMPLEMENTED, BECAUSE THE PLATFORM FEATURE THAT DELIVERS IT IS NOT AVAILABLE ON THIS PROJECT'S PLAN. Recorded on 2026-09-02, during the staff-login build, so this is not later mistaken for an oversight or for a requirement someone quietly dropped. WHAT IS SPECIFIED: `docs/marketprices-design-and-system-architecture.md` §7.1 (~line 732) and `docs/marketprices-build-plan.md` §2.4 (~line 769) both state "12-hour session timeout" as part of the auth requirement, alongside mandatory TOTP 2FA for Admin and Editor and a rate-limited login. WHY IT CANNOT BE SET: Supabase's session controls — maximum session lifetime (time-box) and inactivity timeout — are documented as "only available on Pro Plans and up". The `Marketprices` organisation (`xhvgmaszrgslvtzqpoyl`) was confirmed on 2026-09-02 to be on the **free** plan, so the dashboard setting that would enforce this does not exist for project `marketprices-rebuild` (`wmrojmupdkhswzxhdbmb`). This is a plan gate, not a configuration mistake, and no amount of application code changes what the platform will enforce. NOTE ALSO, AND THIS APPLIES EVEN AFTER AN UPGRADE: Supabase does not proactively terminate sessions when their time-box or inactivity timeout is reached — per the documentation, "these sessions are cleaned up progressively 24 hours after reaching that status". So even on Pro, the timeout is a policy applied at token-refresh time rather than an instant server-side kill, and anyone reasoning about the security properties of "12 hours" should reason about that behaviour rather than the number alone. THE THREE ROUTES, unchanged from when this was raised: upgrade to Pro and set it in the dashboard, which matches the specification exactly and costs money; enforce it in application code by reading the `session_id` claim from the JWT, looking up `auth.sessions.created_at`, and signing out past twelve hours, which works on the free plan but adds a query to every guarded request and moves the guarantee from the platform into our own middleware; or defer. DEFER WAS CHOSEN on 2026-09-02, explicitly and with the trade-off understood, on the grounds that there are currently ZERO rows in `auth.users` and ZERO rows in `profiles` — there is no staff session in existence for a timeout to bound, so the requirement protects nothing today. WHAT THIS MEANS IN PRACTICE UNTIL IT IS REVISITED: sessions follow Supabase's defaults, refresh tokens do not expire, and a signed-in staff session persists until an explicit sign-out, a password change, or a sign-in on another device. REVISIT BEFORE THE FIRST REAL STAFF LOGIN — not before launch, not "in hardening", but before any human other than the developer holds an account, because that is the moment the requirement starts protecting something real. The middleware built on 2026-09-02 (`lib/supabase/middleware.ts`) is the natural home for the application-level variant if that route is taken; it already performs a per-request `profiles` read, so the additional `auth.sessions` lookup would not be the first query in that path.
- [Auth] THE FIRST REAL ADMIN ACCOUNT MUST BE CREATED AS TWO ROWS IN ONE SITTING — an `auth.users` row AND its `profiles` row — AND THE ORDER IS FORCED BY THE SCHEMA. Recorded on 2026-09-02, during the staff-login build, because the login screen alone cannot create this account and anyone who assumes it can will get stuck in a loop that looks like a broken password. THE SHAPE OF THE TRAP: `profiles.id` is `uuid primary key references auth.users (id)` with NO default (`0003_profiles.sql`), so the auth user must exist before the profile row can name it; and `profiles_insert_admin` gates INSERT on `is_admin(auth.uid())`, which reads `profiles`, which is empty — so the very first profile row cannot be inserted by any signed-in user, including the person it is for. Signing up through `/admin/login` therefore produces a valid session with no profile row, which is not a staff session and reaches no admin surface. THE CORRECT ORDER, all of it out-of-band, none of it through the login screen:
  1. Create the `auth.users` row — Supabase Dashboard → Authentication → Users → Add user, or the Admin API `auth.admin.createUser`. Confirm the email as part of this step (the dashboard's auto-confirm option, or `email_confirm: true`); an unconfirmed user cannot sign in with a password and looks identical to a wrong password from the outside.
  2. Copy that user's UUID.
  3. Insert the `profiles` row with the SQL editor or a service-role connection — both bypass RLS, which is the only way past `profiles_insert_admin` while the table is empty. `id` must be the UUID from step 2, `email` must match (it is `not null unique`), `name` is `not null`, and `role` must be set to `'admin'` EXPLICITLY: the column defaults to `'analyst'` by least privilege, and an omitted role silently produces an Analyst who will never be prompted for 2FA and can never create the next account.
  4. NOW sign in at `/admin/login`. Because the role is `admin`, `resolveLoginStep` sends this first session straight to TOTP enrolment; the account is not finished until that factor is verified.
  Every subsequent account is created BY this Admin and does not repeat any of the above — this procedure is for row zero only. WHAT THE CODE DOES ABOUT IT: as of 2026-09-02 `lib/auth/access.ts` models the caller as three states rather than two (`signed-out`, `unprovisioned`, `staff`), and a session with no profile row renders an explicit "Your account isn't fully set up yet" step on the login screen instead of silently re-rendering the credentials form. That message makes the trap legible; it does not remove it, and cannot — the fix is a database insert, not a screen.
- [Auth] `profiles.is_2fa_enabled` AND `profiles.last_login_at` ARE INTENTIONALLY NEVER WRITTEN. Recorded on 2026-09-02, during the staff-login build, so that a later reader finding two columns that are always `false` and always `null` does not file it as an unfinished feature and go "fix" it. Both columns exist in `0003_profiles.sql` and both are, as of this date, written by nothing: no application code, no trigger, no job. THIS IS THE DESIGN, FOR ONE REASON: Supabase already holds these facts, and its copy is the authoritative one. Two-factor status is derived live from `auth.mfa.getAuthenticatorAssuranceLevel()` and the enrolled-factor list — `lib/auth/access.ts` reads `nextLevel === "aal2"` to mean "has a verified factor" — which reflects the actual state of the actual factor at the moment of the request. A mirrored `is_2fa_enabled` boolean could only ever be a cached assertion about someone else's records, and a cached assertion about a security control is worse than no assertion: it goes stale silently, it can be wrong in the permissive direction, and the moment it disagrees with Supabase the disagreement is invisible until someone is either locked out or let in wrongly. The same argument applies to `last_login_at`, whose truth lives in `auth.users.last_sign_in_at`. NOTE the one place `is_2fa_enabled` is not inert: `protect_profile_privileges()` (`0003_profiles.sql`, re-hardened in `0027_function_hardening.sql`) refuses to let a non-admin change it on their own row. That guard stays, and stays correct, precisely BECAUSE the column is never legitimately written — it costs nothing and it closes the hole if the column is ever put into use. SO: THIS IS NOT A GAP TO CLOSE LATER. Do not add a write to either column to "complete" the profile, and do not read either one to make an authorisation decision — `resolveAdminAccess` and `resolveLoginStep` deliberately consult neither. IF A REASON TO POPULATE THEM EVER APPEARS — a staff-directory display, an inactivity report — treat them as a denormalised cache with a named writer and a stated staleness tolerance, and say so here; do not let them quietly become a second source of truth about 2FA.
- [Dashboard] THE DISPATCH FAILURES CARD IS UNMEASURED BY DESIGN, AND ZONE 1 THEREFORE NEVER COLLAPSES. Recorded 2026-09-04 with Stage 6. §8.10 defines the card as `social_content_queue.status = failed` after three attempts. That table does not exist in this database (see the `social_content_queue` entries above), so the count cannot be taken. `lib/queries/dashboard.ts` returns it as `{ state: "unavailable", label: "Not connected yet", note: … }` and the card renders those words where the figure would be. IT MUST NOT READ 0. A zero on the one card meaning "something you published did not go out" is the most expensive false reassurance on the screen, and it is exactly the failure P0.2 names. THE CONSEQUENCE IS DELIBERATE: `shouldCollapseQueueZone` requires all six cards to be MEASURED zeroes, so the "Nothing needs you right now." line §8.10 asks for is unreachable in production today — five genuine zeroes plus one unknown is not "nothing needs you", it is "five queues are empty and one is unreadable". The collapsed state is still built and unit-tested (`lib/queries/dashboard.test.ts`, `components/admin/queue-zone.test.tsx`) so that the rule is fixed now rather than written later against a screen nobody can see fail. It starts working, with no code change, the day the dispatch queue is connected. DO NOT "fix" this by defaulting the count to 0 to make the empty state reachable.
- [Dashboard] VERIFICATION PASS RATE HAS NO DEFINITION AND RENDERS "AWAITING DEFINITION". Recorded 2026-09-04 with Stage 6. §8.10 asks for a "first-pass percentage, 30d". Nothing in this schema records whether an item passed verification on its FIRST attempt: `content_items.verification_log` is jsonb whose shape migration 0018 deliberately leaves unenforced and undocumented, and there is no attempt counter, no per-pass row and no failure timestamp anywhere else. Any percentage computed today would be a guess with a denominator invented to suit it (P0.3, P0.2). So the slot is BUILT — it holds its place in the six-readout strip, in spec order — and prints its reason instead of a number. WHAT UNBLOCKS IT: a decided shape for the verification record, which belongs with the article production chain (Stage 15), not with the Dashboard. When that lands, only `readVerificationPassRate()` changes; the readout's shape does not.
- [Dashboard] THIS WEEK'S MIX IS COUNTED FOR REAL AND CARRIES NO TARGET COMPARISON. Recorded 2026-09-04 with Stage 6. §8.10 draws it as "2 articles · 1 video" against the Settings targets, amber when over or under. The counts are real — published `content_items` by format, inside the current ISO week read on the Lagos clock. The COMPARISON is not built, because the weekly article target and video cap live in Settings group 8 (`editorial_rules`), that table is empty, it has no seed, and no shape for a target row has been agreed; Settings is Stage 17. The spec's "2 · 1" is an illustration and copying it would make the amber state a fiction. THE QUALIFIER IS UNCONDITIONAL ON PURPOSE: an earlier draft probed `editorial_rules` and dropped the caveat when any active rule row existed, which would have made the readout quietly stop saying it was uncompared while still showing no comparison. The note and the comparison get built in the same commit, or neither does. `attention` stays false for the same reason — an amber dot derived from a target nobody set is decoration.
- [Dashboard] TWO §7.5 SIDEBAR VALUES WERE UNSPECIFIED AND BOTH RESOLVE TO `--navy-brand`. Recorded 2026-09-04 with Stage 6. §7.5 describes the active nav item as carrying "a slightly lighter navy background" without naming it, and names a badge fill for Price radar only (`--fall`, because anomalies are urgent) while leaving Signal feed, Draft studio and Publish queue unstated. Rather than invent a colour (CLAUDE.md: NEVER invent values), both were resolved to `--navy-brand` — the palette's one lighter navy, and already the ramp step above `--navy-deep` in §7.4's severity scale. Added to `styles/tokens.css` as `--sidebar-active-bg`, `--badge-count-bg` and `--badge-urgent-bg`, all three as aliases of existing colours so no new hex enters the palette. NOTE on `--fall` in a badge: §7.4 forbids direction and severity sharing a channel; this badge is neither, and is a single fixed urgency mark on one nav item rather than a scale, so it cannot be read as a falling price. If §7.5 is ever amended with real values, change the three tokens and nothing else.
- [Dashboard] ZONE 1 IS SIX-ACROSS ONLY AT 1440 AND WIDER; BELOW THAT THE ROW WRAPS. Recorded 2026-09-04. §8.10 says "Six count cards in one row" and the comps are 1440, which is the width at which that holds. `components/admin/queue-zone.tsx` therefore switches to six columns at `min-[1440px]`, three at `min-[768px]`, one below. THIS IS A MEASURED THRESHOLD, NOT A PREFERENCE: at 1200px six columns leave each card about 139px wide, and the Dispatch failures card — a short label plus a one-sentence explanation, where the other five hold a single digit — overflowed its fixed 96px box by 74px and was silently clipped. The first draft of this grid used `min-[1200px]` and shipped that defect. `tests/e2e/dashboard-queue-card.spec.ts` measures both widths in a real browser and fails if any card's content exceeds its box or if the six figures stop sharing a baseline; it was verified to fail against the pre-fix component and pass against the current one. IF A LATER CHANGE WANTS SIX ACROSS BELOW 1440, the card's fixed height or the unavailable state's content has to change first, and that test will say so.
- [Dashboard] LAYOUT DEFECTS IN ZONE 1 ARE NOT REACHABLE BY THE UNIT SUITE. Recorded 2026-09-04. The 96px queue card holds variable-length text and its `unavailable` state holds three pieces where the other five hold two, so whether it fits is a browser measurement, not a markup fact. All 153 unit tests passed while "Not connected yet" was clipped in half and the note read "...lives outside this" — `renderToStaticMarkup` assertions cannot see it, and `toContainText` passes against fully-escaped text because every character is still in the DOM. The guard is `tests/e2e/dashboard-queue-card.spec.ts`, which renders the REAL component out of process (Playwright's transform compiles JSX with its own component-test factory and cannot hand it to react-dom/server, hence the subprocess in tests/e2e/fixtures/), mounts it on a standalone document carrying the app's compiled stylesheets, and compares rectangles. NOTE the trap the first version of that test fell into: it injected markup into a live `/admin/login` page, which races Next's hydration — the route re-renders and throws the injection away, so the test passed alone and failed in a full run. It now replaces the document wholesale so no client bundle survives to overwrite it. Do not "simplify" it back to injecting into a live page.
- [Stage 3 / Ingest] THE MAGNITUDE OUTLIER CHECK DOES NOT RUN ACROSS A UNIT CHANGE, AND NOTHING ON THE SUBMISSION SAYS SO. Recorded 2026-09-07. `computeFlags` in `lib/validation/ingest.ts` compares a submitted price to the last live `price_observations` row for the same commodity and tier, and raises `outlier` at the seeded 4x ratio (`editorial_rules` ingest/magnitude_ratio). WHEN THE SUBMITTED `unit_id` DIFFERS FROM THAT ROW'S, NO RATIO EXISTS AND NO FLAG IS RAISED — a price per sack against a price per plate is not a multiple of anything. Converting is not available either: `units.base_multiplier` is NULL on all 8 seeded units (see 0036 and seed.sql — "not yet weighed"), so there is no legal path from one unit to another, and inventing one would be a fabricated derivation (P0.2). Raising `outlier` anyway would assert a check that did not happen; that is why the branch is deliberately empty rather than defensive. THE CONSEQUENCE IS A REAL BLIND SPOT: a submission that changes unit AND carries an extra zero reaches the review queue with an empty `flags` array, indistinguishable from one that was checked and found ordinary. The reviewer's only defence today is the unit column in the queue row. THE FIX NEEDS A MIGRATION, NOT A CODE CHANGE: `price_submissions.flags` has a CHECK constraining it to `outlier`, `new_series`, `duplicate`, `site_switch` (0009), so a `unit_change` value — meaning "this could not be compared, look yourself" — has to be added there before the route can emit it. Do this when real weights come back from the field and `base_multiplier` is populated, because at that point the better answer is available too: convert both prices to the base unit and run the ordinary ratio check, leaving `unit_change` for the cases still unconvertible. Until then this is known and accepted, not overlooked. Covered by the test "does not claim an outlier check it could not perform across a unit change" in `lib/validation/ingest.test.ts`, which pins the current behaviour so a future change to it is deliberate.
- [Auth] A SECOND ACCOUNT NOW EXISTS AND NO HUMAN HOLDS IT — A CONTRIBUTOR SERVICE ACCOUNT FOR CI, CREATED 2026-09-15. Recorded because the account model this schema is built around is invite-only and one-account-per-person (`0003_profiles.sql`: "only an admin can create a profile row (invite-only)"), and a robot account is a deviation from that whether or not anyone minds. WHAT IT IS: `profiles.name` = "CI end-to-end", `role` = `contributor`, `is_active` = true, email confirmed, ZERO verified TOTP factors. WHY THAT ROLE AND NOT ANOTHER: it is the only one of the four for which an unattended login is possible at all. Admin and Editor mandate a second factor (§7.1, P9.4, `lib/auth/roles.ts`), and nothing that could compute a TOTP code is stored anywhere — deliberately, and `captureSession` fails fast rather than hanging when it meets a challenge it cannot answer. An Analyst sees one nav item (§7.2) and so cannot exercise the sidebar the test is there to check. Contributor is therefore the least privilege that still renders the surface under test. HOW IT WAS CREATED: by hand, in two rows, exactly as the row-zero entry above describes — an `auth.users` row with the email confirmed, then a `profiles` row naming that UUID with `role` set EXPLICITLY. That entry says "every subsequent account is created BY this Admin and does not repeat any of the above", and AS OF THIS DATE THAT IS STILL NOT TRUE OF THIS CODEBASE: the Users & roles screen it refers to is Settings group 1 (§8.16), which is stage 17 and unbuilt, so there is no invite flow and the hand procedure remains the only mechanism for account two as it was for account one. WHAT IT CAN REACH, which is the part worth knowing before anyone decides this is harmless: every SELECT policy the Dashboard passes through gates on `is_staff()`, which is `role`-agnostic by construction (`0008_collectors.sql`: "Deliberately broader than is_admin_or_editor() (contributors/analysts count too)"), so this account reads everything any member of staff reads — INCLUDING `collectors` WITH `phone`, which is the table that fills with PII first and currently has zero rows. On the write side `can_promote()` and `can_author()` both admit `contributor`, so it can insert `content_items` it owns at status `queued`, update those same items short of `scheduled`, insert `tags`, and insert or update its own `media`. That is the blast radius of the GitHub secret, stated plainly rather than discovered later. THE KILL SWITCH IS `is_active`: every one of those policies runs through `is_staff()` or a sibling that tests it, so setting `is_active = false` on this row ends the account's access everywhere at once, with no code change and no deploy. DO NOT PROMOTE THIS ACCOUNT. Raising it to Editor or Admin would not merely widen it — it would make its own CI job fail, because the capture would then demand a second factor that does not exist.
- [Auth] THE SESSION-TIMEOUT DEFERRAL ABOVE IS NOW LOAD-BEARING IN A WAY IT WAS NOT WHEN IT WAS WRITTEN, AND ITS STATED REASON HAS EXPIRED. Recorded 2026-09-15, as a correction to the 12-hour-timeout entry above rather than an edit to it, per the fix-forward posture this file has used since 0023. THAT ENTRY DEFERRED THE REQUIREMENT "on the grounds that there are currently ZERO rows in `auth.users` and ZERO rows in `profiles` — there is no staff session in existence for a timeout to bound, so the requirement protects nothing today." THAT SENTENCE IS NO LONGER TRUE. `auth.users` has held the Admin account since 2026-09-02, and as of 2026-09-15 it also holds the CI service account described in the entry above. The decision may well still be the right one; what is no longer available is the reason given for it, and a deferral whose justification has quietly lapsed is exactly the kind of thing this file exists to stop becoming invisible. WHAT ACTUALLY CHANGED IN THE RISK: the original entry's own closing line said "REVISIT BEFORE THE FIRST REAL STAFF LOGIN — not before launch, not 'in hardening', but before any human other than the developer holds an account, because that is the moment the requirement starts protecting something real." The CI account is not a human, so that trigger has NOT strictly fired. But it sharpens the case in a way the original could not have anticipated: this is a long-lived, unattended, password-only (aal1) session belonging to nobody, whose password sits in GitHub Actions secrets, and under the current settings "refresh tokens do not expire, and a signed-in staff session persists until an explicit sign-out, a password change, or a sign-in on another device". A stolen session for an account no person watches is precisely the case a time-box bounds and nothing else in this system does. THE THREE ROUTES ARE UNCHANGED — upgrade to Pro and set it in the dashboard; enforce it in `lib/supabase/middleware.ts` by reading `session_id` from the JWT and checking `auth.sessions.created_at`; or keep deferring. NOTHING IS BEING DECIDED HERE and no code changed: this entry exists so that the next person to read the deferral reads its correction in the same breath, rather than finding a now-false premise and taking it at face value. THE TRIGGER STANDS AS WRITTEN: before the first staff account held by a human other than the developer.
