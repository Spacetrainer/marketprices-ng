- [Stage 1] CLAUDE.md summary lists "grid 972px" but architecture §13 defines no --grid token (972 appears nowhere). tokens.css written faithful to §13. Reconcile before the first component that uses the main content grid: either add --grid:972px to §13 then tokens.css (per P6.2, doc first), or drop "grid 972px" from the CLAUDE.md summary. Decision pending.
- [Primitives] Focus rings on Button/FilterPill rely on the browser's native :focus-visible outline — no outline: none is set anywhere, which satisfies P7.5 literally, but no dedicated focus-ring token exists in tokens.css and none was invented. Revisit at Stage 21 to decide whether focus rings get an explicit token (e.g. an amber ring) instead of the native default.
- [Primitives] Button's spec padding (§4.7) is "10px 24px". --sp-6 (24px) matches the horizontal value exactly; no --sp-* token matches 10px (scale jumps --sp-2:8px → --sp-3:12px). Implemented as an arbitrary py-[10px] literal (not a color, so P6.1's hex-ban doesn't apply) rather than rounding to an existing token and drifting from spec. Reconcile if/when a token is added for this value.
- [Layout chrome] tokens.css has no --breakpoint-* tokens at all. §11.2's four breakpoints (1440/1200/992/768) and §4.8's two nav-spacing values (36px/28px) exist only as prose, not tokens. Nav and the chrome components use them as literal arbitrary Tailwind values (min-[992px]:, max-[767px]:, gap-[28px], gap-[36px]) — spec-sourced, not invented, but not reconciled into tokens.css/@theme as reusable breakpoint tokens yet. Reconcile before a second component needs the same breakpoints (per P6.2, doc first).
- [Layout chrome] `pnpm test:e2e` (Playwright) cannot run in this Codespace sandbox — the downloaded Chromium binary is missing ~20 system libraries, and apt can't locate most of them without a package-index refresh, which needs elevated/unsandboxed permission this session doesn't have. The suite (tests/e2e/chrome.spec.ts) is written and kept; it runs in CI instead (.github/workflows/ci.yml), where `playwright install --with-deps` works normally on the GitHub-hosted runner. Verified locally via pnpm dev + compiled-CSS/DOM inspection instead — see the §1.4 commit message/PR notes for what was checked.
- [Entitlements] api_key_hash and api_rate_limit_per_day, both originally planned for the entitlements table, are dropped entirely from 0004_entitlements.sql. API-key issuance/storage and rate-limit enforcement are deferred to a future api_keys table — no client-facing API surface exists yet to consume either field, and storing a hash column with nothing populating or checking it is worse than not having it. Revisit when the API-key feature is actually built: add api_keys (key_hash, entitlement_id/user_id, rate_limit_per_day, ...) as its own migration rather than reviving these columns on entitlements.
